# Required for the AI Assistant (app/api/chat/route.ts) to work.
# Get a key at https://platform.openai.com/api-keys
# (Swap the SDK import in route.ts if you'd rather use Anthropic/another provider.)
OPENAI_API_KEY=

# Required for the Contact form (app/api/contact/route.ts) to send email via Resend.
# Get a key at https://resend.com/api-keys
RESEND_API_KEY=
CONTACT_TO_EMAIL=2025.khushis@isu.ac.in
